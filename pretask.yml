
# - hosts: localhost
#   connection: local
#   gather_facts: false
#   user: root
#   pre_tasks:
#    - include_vars: ec2_vars/{{type}}.yml
#   roles:
#    - provision-ec2 

 - name: Pretask
   remote_user: root
   gather_facts: True
   roles:
    - common
